<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Edit Campground</title>
</head>
<body>
	
	<form action="/campgrounds/<%= campground.id %>?_method=PUT" method="POST">
		<div>
		<label for="title">Title</label>
		<input type="text" id="title" name="campground[title]" value="<%= campground.title %>">
	</div>
	
	<div>
		<label for="location">Location</label>
		<input type="location" id="location" name="campground[location]" value="<%= campground.location %>">
	</div>
	
	<button>Update Campground</button>	
	</form>
	<a href="/campgrounds/<%=campground._id%>">Cancel</a>
</body>
</html>